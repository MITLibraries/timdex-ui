<%= content_for(:title, "Search Results | MIT Libraries") %>

<%= render(partial: 'shared/error', collection: @errors) %>

<% if @record.nil? %>
	<%= render('record_empty') %>
<% else %>
	<%= render('record') %>
<% end %>
