<% return unless @enhanced_query[:isbn].present? %>

<% data_url = "/isbn?isbn=#{URI.encode_www_form_component(@enhanced_query[:isbn])}" %>

<div class="fact-container"
     data-controller="content-loader"
     data-content-loader-url-value=<%= data_url %>>
</div>
